# Delivery Emulator Configuration
# Copy to .env and adjust values

# PostgreSQL connection (same DB as main backend, tables use emul_ prefix)
DATABASE_URL=postgresql+asyncpg://vkus:vkus@localhost:5432/vkus

# Server binding
HOST=0.0.0.0
PORT=8001

# Logging level
LOG_LEVEL=INFO
